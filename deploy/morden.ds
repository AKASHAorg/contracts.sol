var registry = new AkashaRegistry()
var helper = new AkashaLib()
var tags = new AkashaTags(helper)
var indexedTags = new IndexedTags(tags, registry)
export registry
export helper
export tags
export indexedTags
